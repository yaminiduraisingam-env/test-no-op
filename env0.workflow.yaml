environments:
  env-a:
    name: env-a
    templateName: no-op-producer

  env-b:
    name: env-b
    templateName: no-op-consumer
    needs:
      - env-a
    variables:
      - name: received_value
        value: ${env0-workflow:env-a:shared_value}
